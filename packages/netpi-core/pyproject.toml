[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "netpi-core"
version = "0.1.0"
description = "NetPi core models, enums, and schemas"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[tool.hatch.build.targets.wheel]
packages = ["netpi_core"]
